# Remote Terminal

本上下文描述浏览器经公网中继安全访问私有网络内电脑终端时使用的统一语言。

## 身份与设备

**用户（User）**：
登录网页并被授权访问设备与会话的人。
_避免_：账号、操作者

**设备（Device）**：
已配对且可由用户远程访问的一台电脑。
_避免_：主机、节点、机器

**Agent**：
以本地用户身份运行在设备上、主动连接中继并管理本地会话的守护进程。
_避免_：客户端、探针、远端服务

**配对（Pairing）**：
用户把一个 Agent 的设备身份绑定到自己的短时授权过程。
_避免_：注册、登录

## 连接与会话

**中继（Relay）**：
部署在公网、负责鉴权并转发浏览器与 Agent 流量的系统。
_避免_：代理、跳板机、终端服务器

**隧道（Tunnel）**：
Agent 主动建立并维持到中继的安全出站连接。
_避免_：反向 SSH、端口映射

**Shell 配置（Profile）**：
设备预先声明的可启动 Shell 及其初始目录、环境与终端选项集合。
_避免_：模板、预设、命令

**会话（Session）**：
Agent 上一个 Shell 进程、其 PTY 及生命周期组成的可重连终端实例；浏览器断开不等于会话结束。
_避免_：连接、标签页、终端窗口

**附着（Attachment）**：
一个浏览器视图到既有会话的临时连接；同一会话可以同时有多个附着。
_避免_：会话、Socket

**控制者（Controller）**：
当前唯一持有输入与尺寸变更权限的附着。
_避免_：管理员、所有者

**观察者（Viewer）**：
只能接收会话输出、不能发送输入或改变 PTY 尺寸的附着。
_避免_：访客、只读用户

## 数据与治理

**输出日志（Journal）**：
Agent 为断线续传保留的有界、按序终端输出数据。
_避免_：录屏、审计日志、滚动缓冲区

**录制（Recording）**：
经用户明确启用、包含带时间信息终端输入输出的持久内容。
_避免_：审计、输出日志

**审计事件（Audit Event）**：
描述登录、配对、会话、传输、分享和设置变更的安全元数据；默认不包含命令、终端内容或环境变量。
_避免_：录制、运行日志
